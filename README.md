# sec terminal

Portofolio Security bertema terminal (Next.js App Router + Tailwind).

## Run
```bash
npm install
npm run dev
```

## Customize
Edit data in `lib/data.ts`.
Drop your CV in `public/cv.pdf` (optional).
